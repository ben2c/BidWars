<h1>Post an Item</h1>

<%= form_for(@item) do |f| %>

    <%= f.label :title %>
    <%= f.text_field :title %>

    <%= f.label :description %>
    <%= f.text_field :description %>

    <%= f.submit "Post Item" %>

<% end %>